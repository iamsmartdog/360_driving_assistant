# 数据库设计文档

## 数据库选择

本项目使用 MySQL 作为数据库管理系统。

## 表结构设计

### users 表（用户信息）

| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | INT | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(255) | 密码（加密存储） |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### driving_records 表（行车记录）

| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | INT | 主键，自增 |
| user_id | INT | 用户ID（外键） |
| start_time | TIMESTAMP | 开始时间 |
| end_time | TIMESTAMP | 结束时间 |
| distance | DECIMAL(10, 2) | 行车距离 |

### warnings 表（报警记录）

| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | INT | 主键，自增 |
| record_id | INT | 行车记录ID（外键） |
| warning_type | VARCHAR(50) | 报警类型 |
| warning_level | INT | 报警等级 |
| timestamp | TIMESTAMP | 报警时间 |
| description | TEXT | 报警描述 |

## TODO

- [ ] 根据需求文档完善表结构
- [ ] 设计索引策略
- [ ] 设计数据备份方案
- [ ] 定义数据访问接口（DAO层）